PORT=3001

# CouchDB (Railway: usar URL pública del servicio CouchDB)
COUCHDB_URL=http://localhost:5984
COUCHDB_ADMIN_USER=admin
COUCHDB_ADMIN_PASS=password

# CORS: en Railway dejar vacío (el frontend está en el mismo dominio).
# En desarrollo local: http://localhost:5173
FRONTEND_URL=

# Salta el paywall de suscripción SOLO en local. NUNCA poner en Railway/producción.
DISABLE_SUBSCRIPTION_GATE=true
